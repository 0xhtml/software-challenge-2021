#include "Evaluation.h"

int Evaluation::evaluate(GameState gameState) {
    int evaluation = 0;
//    for (auto &x : gameState.board) {
//        for (int value : x) {
//            if (!value) continue;
//            (value - 1) % 2 == gameState.turn % 2 ? (evaluation++) : (evaluation--);
//        }
//    }

    return evaluation;
}
